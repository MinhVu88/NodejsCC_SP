const { people, ages } = require("./people_1");

console.log(people, ages);

const operating_system = require("os");

console.log(operating_system, operating_system.platform(), operating_system.homedir());
